services:
  db:
    image: postgres:15
    restart: always
    env_file:
      - .env
    ports:
      - "5000:5432"
    volumes:
      - ./table.sql:/docker-entrypoint-initdb.d/init.sql
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: